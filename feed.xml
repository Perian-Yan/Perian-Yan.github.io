<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://perian-yan.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://perian-yan.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-26T08:24:02+00:00</updated><id>https://perian-yan.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Some FFmpeg Commands for ICRA Video</title><link href="https://perian-yan.github.io/blog/2026/FFmpeg-notes/" rel="alternate" type="text/html" title="Some FFmpeg Commands for ICRA Video"/><published>2026-09-23T00:00:00+00:00</published><updated>2026-09-23T00:00:00+00:00</updated><id>https://perian-yan.github.io/blog/2026/FFmpeg-notes</id><content type="html" xml:base="https://perian-yan.github.io/blog/2026/FFmpeg-notes/"><![CDATA[<p>This note wrties some FFmpeg command I used when preparing ICRA 2027 supplementary video.</p> <h2 id="official-document">Official document</h2> <p>[https://ffmpeg.org/documentation.html]</p> <h2 id="download">Download</h2> <p>After downloading it in Windows, we get three executables: bin/</p> <ul> <li>ffmpeg.exe (video conversion and processing)</li> <li>ffprobe.exe (inspect video informaion)</li> <li>ffplay.exe (play the video)</li> </ul> <h2 id="add-ffmpeg-to-the-path">add ffmpeg to the path</h2> <p>Let the Bash know where ffmpeg.exe is, and no need to write the full path any more.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nano ~/.bashrc
</code></pre></div></div> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">PATH</span><span class="o">=</span><span class="s2">"</span><span class="nv">$PATH</span><span class="s2">:/c/Users/JXZ/Downloads/ffmpeg-master-latest-win64-gpl/ffmpeg-aster-latest-win64-gpl/bin"</span>
</code></pre></div></div> <p>Verify after <code class="language-plaintext highlighter-rouge">source ~/.bashrc</code> :</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># shows the above directory</span>
which ffmpeg
</code></pre></div></div> <p>or</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ffmpeg <span class="nt">-version</span>
</code></pre></div></div> <h2 id="video-conversion">video conversion</h2> <p>The experimemt videos collected via ROS node is encoded by MJPEG, which cannot be opened by the default palyer on Windows (can be opened by VLC), nor can it be decoded in Davinci Resolve 21. MJPEG, however, is suitable for image acquisition and frame-by-frame processing, since each frame is a JPEG image and independently compressed. In order to open the video in Davinci, the codec needs to be changed from mjpeg to h264.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ffmpeg <span class="nt">-i</span> <span class="s2">"input.mp4"</span> <span class="se">\</span>
<span class="nt">-c</span>:v libx264 <span class="se">\</span>
<span class="nt">-crf</span> 18 <span class="se">\</span>
<span class="nt">-preset</span> medium <span class="se">\</span>
<span class="nt">-pix_fmt</span> yuv420p <span class="se">\</span>
<span class="nt">-an</span> <span class="se">\</span>
<span class="s2">"output_h264.mp4"</span>
</code></pre></div></div> <ul> <li>Transcoding: <code class="language-plaintext highlighter-rouge">-c:v libx264</code> <ul> <li>decoding a <strong>video</strong> stream and encoding it by <strong>H.264 encoder</strong></li> </ul> </li> <li><code class="language-plaintext highlighter-rouge">-crf 18</code> constant rate factor. <ul> <li>the lower the number, the higher the quality. 18 is a high quality.</li> </ul> </li> <li><code class="language-plaintext highlighter-rouge">-preset medium</code> <ul> <li>slow preset means more computation time for the encorder to find a efficient compression way. This means to achieve the same quality but use less bitrate.</li> </ul> </li> <li><code class="language-plaintext highlighter-rouge">pix_fmt yuv420p</code> pixel format</li> <li><code class="language-plaintext highlighter-rouge">-an</code> skip the audio stream in the output. Audio None.</li> </ul> <p><strong>batch conversion</strong></p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> h264
</code></pre></div></div> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for </span>f <span class="k">in </span>experiment<span class="k">*</span>.mp4<span class="p">;</span> <span class="k">do
    </span>ffmpeg <span class="nt">-i</span> <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span> <span class="se">\</span>
        <span class="nt">-c</span>:v libx264 <span class="se">\</span>
        <span class="nt">-crf</span> 18 <span class="se">\</span>
        <span class="nt">-preset</span> medium <span class="se">\</span>
        <span class="nt">-pix_fmt</span> yuv420p <span class="se">\</span>
        <span class="nt">-an</span> <span class="se">\</span>
        <span class="s2">"h264/</span><span class="k">${</span><span class="nv">f</span><span class="p">%.mp4</span><span class="k">}</span><span class="s2">_h264.mp4"</span>
<span class="k">done</span>
</code></pre></div></div> <p>For instance, <code class="language-plaintext highlighter-rouge">$f</code> is <code class="language-plaintext highlighter-rouge">experiment01.mp4</code>, then <code class="language-plaintext highlighter-rouge">${f%.mp4}</code> gives <code class="language-plaintext highlighter-rouge">experiment01</code> by removing the suffix <code class="language-plaintext highlighter-rouge">.mp4</code>.</p> <p>Davinci Resolve exports clips as <code class="language-plaintext highlighter-rouge">.mov</code>, using the following command to change it to <code class="language-plaintext highlighter-rouge">.mp4</code>. Only change the container while keeping the codec.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ffmpeg <span class="nt">-i</span> <span class="s2">"input.mov"</span> <span class="nt">-c</span> copy <span class="s2">"output.mp4"</span>
</code></pre></div></div> <h2 id="video-compression">video compression</h2> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ffmpeg <span class="nt">-i</span> <span class="s2">"ICRA_master.mov"</span> <span class="se">\</span>
<span class="nt">-c</span>:v libx264 <span class="se">\</span>
<span class="nt">-b</span>:v 800k <span class="se">\</span>
<span class="nt">-preset</span> slow <span class="se">\</span>
<span class="nt">-pix_fmt</span> yuv420p <span class="se">\</span>
<span class="nt">-an</span> <span class="se">\</span>
<span class="nt">-movflags</span> +faststart <span class="se">\</span>
<span class="s2">"ICRA_final.mp4"</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">-b:v 800k</code> denotes the bitrate as 800 kbit/s.</li> <li><code class="language-plaintext highlighter-rouge">-movflags +faststart</code> moves the metadata/index to the beginning of the file. This is useful for web playback because playback can start before the entire file has been downloaded.</li> <li>If there is no audio, the goal bitrate can be approximately computed by \begin{equation} bitrate = \frac{8 * size}{duration} \ Mbps \end{equation}</li> </ul> <p>For instance, ICRA requires the final video less than 180 s and 20 MB. \begin{equation} bitrate = \frac{8 * 18}{180} \ Mbps = 0.8 \ Mbps = 800 \ kbps \end{equation}</p> <h2 id="video-check">video check</h2> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ffprobe <span class="nt">-v</span> error <span class="se">\</span>
<span class="nt">-select_streams</span> v:0 <span class="se">\</span>
<span class="nt">-show_entries</span> <span class="nv">stream</span><span class="o">=</span>codec_name,width,height,r_frame_rate,field_order,pix_fmt <span class="se">\</span>
<span class="nt">-of</span> <span class="nv">default</span><span class="o">=</span><span class="nv">noprint_wrappers</span><span class="o">=</span>1 <span class="se">\</span>
<span class="s2">"final.mp4"</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">-v error</code> only prints errors in addition to the requested information.</li> <li><code class="language-plaintext highlighter-rouge">-select_streams v:0</code> selects the first video stream. My final icra video gives <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">codec_name</span><span class="o">=</span>h264
<span class="nv">width</span><span class="o">=</span>1920
<span class="nv">height</span><span class="o">=</span>1080
<span class="nv">pix_fmt</span><span class="o">=</span>yuvj420p
<span class="nv">field_order</span><span class="o">=</span>progressive
<span class="nv">r_frame_rate</span><span class="o">=</span>30/1
</code></pre></div> </div> </li> </ul>]]></content><author><name></name></author><category term="tools"/><summary type="html"><![CDATA[for icra 2027 accompanying video]]></summary></entry><entry><title type="html">How to Build an Academic Webpage</title><link href="https://perian-yan.github.io/blog/2023/how-to-build-an-academic-webpage/" rel="alternate" type="text/html" title="How to Build an Academic Webpage"/><published>2023-09-28T00:00:00+00:00</published><updated>2023-09-28T00:00:00+00:00</updated><id>https://perian-yan.github.io/blog/2023/how-to-build-an-academic-webpage</id><content type="html" xml:base="https://perian-yan.github.io/blog/2023/how-to-build-an-academic-webpage/"><![CDATA[<p>I use the Jekyll theme: <a href="https://github.com/alshedivat/al-folio">al-folio</a> to build my academic page. My system is Windows 10, and I spent some time to finally setup the page on local host and the Github Pages.</p> <p>The following steps are mainly based on <a href="https://github.com/alshedivat/al-folio#getting-started">al-folio Getting Started</a>. Please read through it first.</p> <h2 id="create-a-new-repo">Create a new repo</h2> <p>I use this template and create a new repository. Steps are <a href="https://docs.github.com/en/repositories/creating-and-managing-repositories/creating-a-repository-from-a-template#creating-a-repository-from-a-template">here</a>. In order to upload the page to my Github Pages later, I need to set the repository name exactly as the username <code class="language-plaintext highlighter-rouge">Perian-Yan.github.io</code>.</p> <p>Once the new repo is created, I git clone it to my local directory:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone git@github.com:Perian-Yan/Perian-Yan.github.io.git
<span class="nb">cd </span>Perian-Yan.github.io
</code></pre></div></div> <h2 id="setup-webpage-locally">Setup webpage locally</h2> <p>First, check if Ruby, Bundler, and Jekyll have been installed on the device by checking the version:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruby <span class="nt">-v</span>
bundle <span class="nt">-v</span>
jekyll <span class="nt">-v</span>
</code></pre></div></div> <figure> <picture> <img src="/assets/img/2023-09-28/image-2.png" class="img-fluid rounded z-depth-1 mx-auto d-block" width="500" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>I haven’t installed Ruby at the beginning, so I downloaded it from <a href="https://rubyinstaller.org/">RubyInstaller</a>.</p> <hr/> <p>When I run <code class="language-plaintext highlighter-rouge">bundle install</code>, an error about mini_racer occurs:</p> <figure> <picture> <img src="/assets/img/2023-09-28/image-3.png" class="img-fluid rounded mx-auto d-block" width="500" height="auto" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>This is because MiniRacer does not support Windows anymore (see <a href="https://github.com/rubyjs/mini_racer#supported-ruby-versions--troubleshooting">MiniRacer</a>).</p> <p>The problem, however, can be resolved by commenting out mini_racer and adding <code class="language-plaintext highlighter-rouge">gem 'wdm', '~&gt; 0.1.0'</code> in the Gemfile, and installing <a href="https://nodejs.org/en/download">Node.js</a>, see this <a href="https://github.com/alshedivat/al-folio/issues/691#issuecomment-1309072582">thread</a> for more details.</p> <hr/> <p>The next step is to install Jupyter, otherwise it will cause errors when you run <code class="language-plaintext highlighter-rouge">jekyll serve</code> later:</p> <figure> <picture> <img src="/assets/img/2023-09-28/image-1.png" class="img-fluid mx-auto d-block" width="500" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>I already have conda environment and Jupyter installed. Check the Jupyter’s version in conda environment:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>jupyter <span class="nt">--version</span>
</code></pre></div></div> <hr/> <p>Finally, run <code class="language-plaintext highlighter-rouge">bundle exec jekyll serve</code> (add flag <code class="language-plaintext highlighter-rouge">--trace</code> or <code class="language-plaintext highlighter-rouge">--verbose</code> to show the detail if any errors happen).</p> <p>Before everything going well, I have to disable imagemagick in <code class="language-plaintext highlighter-rouge">_config.yml</code> file (set <code class="language-plaintext highlighter-rouge">enabled: false</code>). Otherwise, I get the error below:</p> <figure> <picture> <img src="/assets/img/2023-09-28/image.png" class="img-fluid mx-auto d-block" width="500" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Now, I can run <code class="language-plaintext highlighter-rouge">bundle exec jekyll serve</code> smoothly and I can connect the local host <code class="language-plaintext highlighter-rouge">https://127.0.0.1:4000</code> to see my webpage.</p> <figure> <picture> <img src="/assets/img/2023-09-28/image-5.png" class="img-fluid mx-auto d-block" width="500" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Success! </div> <h2 id="deploy-webpage-to-github-pages">Deploy webpage to Github Pages</h2> <p>To deploy the website to Github Pages, I just followed the instructions <a href="https://github.com/alshedivat/al-folio#deployment">al-folio Deployment</a>. It will automatically deploy everything, which may take 1-2 minutes. If it fails to deploy, you can run workflow manually.</p> <h2 id="enjoy">Enjoy!</h2> <p>Now everytime I change my page, I can view it locally. To view it in Github Pages, just <code class="language-plaintext highlighter-rouge">git push</code>!</p> <h3 id="edit-config-file">edit config file</h3> <p>Check the <a href="https://www.youtube.com/watch?v=g6AJ9qPPoyc">tutorial video</a> to see how to edit the template.</p> <h3 id="insert-images-in-posts">insert images in posts</h3> <p>The side-effect of disabling imagemagick is that I can not use markdown version to insert images.</p> <div class="language-md highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">![</span><span class="nv">Some text</span><span class="p">](</span><span class="sx">image.png</span><span class="p">)</span>
</code></pre></div></div> <p>The workaround is to follow the <a href="https://alshedivat.github.io/al-folio/projects/1_project/">template</a>, use html and always store images under <code class="language-plaintext highlighter-rouge">assets</code>. More details at <a href="https://getbootstrap.com/docs/5.3/layout/grid/">Bootstrap-Grid</a> and <a href="https://getbootstrap.com/docs/5.3/content/images/">Bootstrap-Images</a>.</p> <h3 id="edit-cv">edit cv</h3> <p>Changing <code class="language-plaintext highlighter-rouge">_data/cv.yml</code> has no effects on the webpage because <code class="language-plaintext highlighter-rouge">resume.json</code> will first be used if it’s defined in <code class="language-plaintext highlighter-rouge">_config.yml</code>. To load the contents of <code class="language-plaintext highlighter-rouge">_data/cv.yml</code>, just commet out lines below “Get external JSON data” in <code class="language-plaintext highlighter-rouge">_config.yml</code>. See this <a href="https://github.com/alshedivat/al-folio/discussions/1611">discussion</a>.</p>]]></content><author><name></name></author><category term="tools"/><summary type="html"><![CDATA[setup Jekyll theme al-folio on Windows]]></summary></entry></feed>